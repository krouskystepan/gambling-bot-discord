// in cmd structure no events => make it different => not tree
// revert back the PROJECT_STRUCTURE => keep tree
import { Client } from 'discord.js'

export default async (client: Client) => {
  console.log(`❌ Blackjack Games listener not implemented`)
}
